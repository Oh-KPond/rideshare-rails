<h2>Drivers List</h2>
<p>This lists all the drivers!</p>

<% @drivers.each do |driver| %>
  <p class="driver">
    <%=  link_to driver.name, driver_path(driver.id), class: 'driver-link' %>
  </p>
  <p class="driver-vin">
    <em><%= driver.vin %></em>
  </p>
<% end %>
